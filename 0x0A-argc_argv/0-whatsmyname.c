#include <stdio.h>

/** 
 * main - program that prints it's name
 * @argc : number of arguments.
 * argv : array thatcontains the arguments
 * Return : 0.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
